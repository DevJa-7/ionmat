import { DatetimemaskDirective } from './datetimemask.directive';

describe('DatetimemaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DatetimemaskDirective();
    expect(directive).toBeTruthy();
  });
});
